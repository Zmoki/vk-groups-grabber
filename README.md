# Grab groups (events, publics) from [vk.com](http://vk.com)

Add file **config.php** with follow code:

```php
<?php

$config = array(
  'mysql' => array(
      'host' => 'YOUR_HOST',
      'database' => 'YOUR_DATABASE',
      'user' => 'YOUR_USER',
      'password' => 'YOUR_PASSWORD'
  )
);
```